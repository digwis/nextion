---
"@notionx/create-nextion-app": minor
"@notionx/core": minor
---

Add typed structured page block support for `hero`, `feature-grid`, and
`story`. The scaffolder now provisions richer Notion block schemas and
renders these reusable blocks through dedicated typed components instead
of relying on generic `NotionBlocks` body content alone.
