---
"create-nextion": patch
---

Rename shim package from `create-nextion-app` to `create-nextion`.

User-facing command shortens from `npm create nextion-app` to
`npm create nextion`, matching the convention used by astro/vite/sveltekit.
